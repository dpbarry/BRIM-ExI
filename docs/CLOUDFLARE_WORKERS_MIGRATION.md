# Cloudflare Workers + GitHub Pages Migration

The implementation now lives in `cloudflare-worker/`.

Use the active runbook at:

- `cloudflare-worker/README.md`

That guide includes:

- D1 creation + migrations
- One-command seed export from local `exi.db`
- Secret configuration
- Worker deploy
- Frontend `config.js` hookup for GitHub Pages
